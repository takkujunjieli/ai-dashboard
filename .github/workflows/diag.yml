name: Massive 权限诊断
on:
  workflow_dispatch: {}
jobs:
  diag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - run: pip install requests
      - run: python scripts/massive_diag.py
        env:
          MASSIVE_API_KEY: ${{ secrets.MASSIVE_API_KEY }}
